pipeline {
    agent any

    parameters {
    }

    environment {
    }

    stages {
    }

    post {
        always {
            script {
        }
        failure {
            echo "Build failed!"
        }
        success {
            echo "Deployment successful!"
        }
    }
}
